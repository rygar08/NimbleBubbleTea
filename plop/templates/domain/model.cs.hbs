using System;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using Volo.Abp.Domain.Entities;

namespace {{ project }}.{{ properCase model.name }}s {
    public class {{ properCase model.name }} : Entity<Guid> { 

        {{#each model.fields as |f|}}
           {{#if f.required}}[Required]{{/if}}  
           public {{f.type}} {{ properCase f.name}} { get; set; }  
        {{/each}} 


        public {{ properCase model.name }}(Guid id) : base(id) {
        }
    }
}
